[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn server.app:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health"
healthcheckTimeout = 300   # allow time for first-boot auto-training
restartPolicyType = "on_failure"
